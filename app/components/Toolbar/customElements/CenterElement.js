import React from 'react';
import { View, Text } from 'react-native';
import styles from '../styles';

const CenterElement = () => (
  <View style={styles.WhiteToolbarTitle}>
    <Text style={styles.WhiteToolbarTitleText}>타이틀</Text>
  </View>
);

export default CenterElement;
